FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV DATABASE_URL=sqlite:////app/data/pricing.db
CMD ["python", "-m", "orchestration.pipelines.run_bootstrap"]
