"""Synthetic multi-chain context/demand generator for the POC.

Same context schema as real data (config/context_schema.yaml) so a real
PMS/CRS/rate-shopper/events ETL can swap in later without touching any
downstream code (bandit engine, guardrails, serving API, dashboard)."""
